// Press CTRL+S or Cmd+S to save. The system will check for syntax errors.
// After specifying the input, press Run to see the execution results.

#include <bits/stdc++.h>
using namespace std;
int main() {
  int a;
  int b;
  cin>>a;
  cin>>b;
  cout<<a+b;
  return 0;
}